dataset_id: Call_CDR
dataset_version: 3
description: Cells per LAC_ID
field: START_CELL_1_ID
id: START_CELL_1_ID-CELLS_PER_LAC_ID
id_jira: val-288
metrics:
- distribution
product: SmartSteps
sources:
- dataset_id: Cells_Catalogue
  dataset_version: 5
  name: Cells_Catalogue
sql: SELECT to_json(collect_list((LAC_ID, RESULT))) AS distribution FROM (SELECT LAC_ID,COUNT(*)
  AS RESULT FROM (SELECT * FROM validation_data_table t1 LEFT OUTER JOIN Cells_Catalogue
  t2 ON t1.START_CELL_1_ID=t2.CELL_ID) GROUP BY LAC_ID)
type: '0'
