dataset_id: Data_XDR
dataset_version: 4
description: Distribution of number of events by hour per feed
field: EVENT_TM
id: EVENT_TM-countEventsByHour
id_jira: val-117
metrics:
- distribution
product: SmartSteps
sources: []
sql: SELECT to_json(collect_list((HOUR,TOTAL))) as distribution FROM (SELECT substring(EVENT_TM,10,2)
  AS HOUR, COUNT(*) AS TOTAL FROM validation_data_table GROUP BY HOUR ORDER BY HOUR)
